using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeftHandModel : MonoBehaviour
{
    //public Transform Cube7;
    void Start()
    {
      //  Cube7 = GameObject.Find("Cube7").transform;
    }

    /*
    // Update is called once per frame
    void Update()
    {
        Cube7.transform.position = transform.position;
        Cube7.transform.rotation = Quaternion.Euler(new Vector3(Cube7.transform.eulerAngles.x, transform.eulerAngles.y, Cube7.transform.eulerAngles.z));
    }
    */
}
